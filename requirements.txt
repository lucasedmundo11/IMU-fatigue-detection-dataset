# Data processing layer. Modelling dependencies come later.
numpy>=1.21
scipy>=1.7
pandas>=1.3

# Exploration notebooks under notebooks/. Not needed by `python -m src`.
matplotlib>=3.5
seaborn>=0.12
ipykernel>=6
nbconvert>=7   # only to re-execute a notebook headlessly (see NOTES.md)

# Optional: lets `export-tablefeats --out ...parquet` write parquet instead of
# falling back to csv.
# pyarrow>=10
